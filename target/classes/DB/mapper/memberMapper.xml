<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.iu.s5.member.MemberDAO">

<sql id="search">
	like '%'||#{search}||'%'
</sql>

<!-- 검색 -->
<select id="memberCount" parameterType="Pager" resultType="MemberVO"><!-- 단축키(mybatisConfig): 마이바티스에경우에만 해당, java쪽은 상관 x -->
		select count(rownum) from member
		<where>
			<if test="kind=='id'">
				id <include refid="search"></include>
			</if>
			
			<if test="kind=='name'">
				name <include refid="search"></include>
			</if>
			
			<if test="kind=='phone'">
				phone <include refid="search"></include>
			</if>
			
			<if test="kind=='email'">
				email <include refid="search"></include>
			</if>
		</where>
</select>


	<select id="memberList" parameterType="Pager" resultType="MemberVO">
	select * from
		(select N.*, rownum R from 
			(select * from member where
			<choose>
				<when test="kind=='id'">
					id
				</when>
				
				<when test="kind=='name'">
					name
				</when>
				
				<when test="kind=='phone'">
					phone
				</when>
				
				<otherwise>
					email
				</otherwise>
			</choose>
			<include refid="search"></include> order by rownum desc) N)
			where R between #{startRow} and #{lastRow}
	
	</select>

	<select id="memberLogin" parameterType="MemberVO" resultType="MemberVO">
		select * from member where id=#{id} and pw=#{pw}
	</select>
	
	
	<insert id="memberJoin" parameterType="MemberVO">
		INSERT INTO MEMBER VALUES (#{id},#{pw},#{name},22,#{phone},#{email})
	</insert>
	
	<delete id="memberDelete" parameterType="MemberVO">
		delete member where id=#{id}
	</delete>
	
	<update id="memberUpdate" parameterType="MemberVO">
		update member set name=#{name},age=#{age},phone=#{phone},email=#{email} where id=#{id}
	</update>
	
	</mapper>